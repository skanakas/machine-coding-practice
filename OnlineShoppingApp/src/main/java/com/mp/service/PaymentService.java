package com.mp.service;

import com.mp.account.fundingSource.FundingInstrument;
import com.mp.model.account.Member;
import com.mp.model.order.Order;

public class PaymentService {
	
	public boolean processPayment(Member user, Order orderDetail, FundingInstrument payment) {

		/**
		 * Based on the funding instrument create the payment activity object and complete the payment
		 */
		
		return true;
	}

}
